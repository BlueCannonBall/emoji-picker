[paths]
output = "emoji-picker"
source = ["."]
artifact = "obj"
install = "/usr/local/bin"

[options]
compilation-flags = "-Wall -std=c++20 -Ifltk/build -O3 -pthread"
link-time-flags = "`fltk/build/fltk-config --use-images --ldstaticflags`"
libraries = ["xdo"]
pkg-config-libraries = ["gio-2.0"]
preludes = [
    """ \
        cd fltk && \
        cmake . -B build -G "Unix Makefiles" -DCMAKE_BUILD_TYPE=Release -DFLTK_BACKEND_WAYLAND=OFF -DFLTK_BUILD_TEST=OFF && \
        "$(MAKE)" -C build \
    """,
]
